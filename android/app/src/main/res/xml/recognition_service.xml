<?xml version="1.0" encoding="utf-8"?>
<!--
  RecognitionService configuration.
  Declares our ability to perform on-device speech recognition.
  This is optional — the system uses Google's RecognitionService by default.
  If we implement a custom RecognitionService in the future, register it here.
-->
<recognition-service
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:settingsActivity="com.jarvis.assistant.SetupActivity" />
